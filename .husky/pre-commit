#!/bin/sh
if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged
else
  echo "gitleaks is not installed on this system. Skipping secrets check."
fi

echo "Running typecheck..."
npm run typecheck

echo "Running lint-staged..."
npx lint-staged
